# Deprecated compatibility pointer. Do not include this file in nginx.
#
# The public service now exposes both Open Mining Protocol v1 remote solo and
# PPLNS Mining Protocol v2. Use the maintained configuration pair instead:
#
#   bitcoin09-open-miner-http.conf    in nginx's http context
#   bitcoin09-open-miner-server.conf  in the canonical TLS server
#
# Keeping the old filename as a pointer prevents an operator from unknowingly
# deploying the retired catch-all /api/v1/ proxy.
