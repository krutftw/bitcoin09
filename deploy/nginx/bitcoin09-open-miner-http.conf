# Load once in nginx's http context. Cloudflare real-IP restoration must be
# configured before these zones are evaluated.
limit_req_zone $binary_remote_addr zone=btc09_miner_work:10m rate=12r/m;
limit_req_zone $binary_remote_addr zone=btc09_miner_submit:10m rate=6r/m;
limit_conn_zone $binary_remote_addr zone=btc09_miner_connections:10m;
